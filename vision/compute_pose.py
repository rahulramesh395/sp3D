import math

class ComputePose:

	def __init__(vision):
		self.vision = vision
		self.pose = None

		#define thresholds
		z_threahold = None
		y_threshold = None
		x_threshold = None

	def _get_center():
		center = self.vision.center_pos
		radius = self.vision.radius

	def _compute_area():
		area = math.pi * self.radius**2
		return area

	def get_position():
		return xyz
